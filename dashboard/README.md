# EdRAM Competitive Intelligence Dashboard

## Démarrage

Depuis la racine du repo (`BDD EdR`), exécutez :

```bash
./start.sh
```

Le script lance automatiquement l'application React/Vite dans `dashboard/`.

## Alternative manuelle

```bash
cd dashboard
npm install
npm run dev
```
